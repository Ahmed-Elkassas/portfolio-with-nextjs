import Layout from "components/layout/Layout";

import { Section } from "styles/global";

const Home = () => {
  return (
    <Layout>
      <Section grid>
          <div>test</div>
      </Section>
    </Layout>
  );
};

export default Home;
